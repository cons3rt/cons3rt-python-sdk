image: python:3.7-alpine

stages:
  - build

before_script:
   - pip install pipenv
   
deploy_staging:
  stage: build
  variables:
    TWINE_USERNAME: banana
    TWINE_PASSWORD: farts
  script:
    - pipenv install --ignore-pipfile
  except:
    - tags